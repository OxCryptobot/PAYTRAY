# PayTray Accelerated Batch Update

**Date:** 2026-08-14  
**Branch:** `paytray/batch-delivery`  
**Remote baseline:** `8498c8643e32691abb12aaa320862dd829e2e3a8`  
**Current state:** Batches AP–AQ, AR–AT, AU–BD, AY, BA, BB, AW, BD, BF, BG, BH, BI, BJ, BL, BM, BN, BO, BP, BQ, BR, BS, BT, and BU are pushed through `6e857f2`. The canonical release payload remains fail-closed pending real approval, authenticated Railway settings, target-environment verifier/recovery evidence, and the operator signing key.

## Delivered in this update

| Batch | Delivered capability | Safety boundary |
|---|---|---|
| **Q — RPC-backed verifier execution** | Added `POST /api/v2/verifier/poll`, explicit `PAYMENT_RPC_URL` gating, configured Base Sepolia worker construction, durable cursor use, and configured-worker readiness status. | Polling cannot run unless an explicit HTTPS RPC URL is configured; the endpoint remains operator-scoped and `shadow_only`. |
| **R — Flow event hydration** | Updated the official Sablier Flow v3 decoder to hydrate token and participant metadata from durable streams for deposit, pause, refund, restart, void, and withdrawal events. BigInt payloads are JSON-safe. | Events without a verified durable stream context are ignored rather than assigned fabricated token or wallet evidence. |
| **S — lifecycle status UX** | Added client “Refresh verified status” behavior for engagement, payment-intent, and chain-finality state. | The client reports state but never claims settlement or mutates payment state. |
| **T — shadow evidence** | Added detailed operator shadow-run evidence at `GET /api/v2/ops/shadow-runs/:runId`, including decisions, counts, reviewer status, and rollback context. | The response explicitly remains `shadow_only` and `human_review_required`; applied decision count is surfaced for audit. |
| **V — chain safety defaults** | Made Base Sepolia (`84532`) the default settlement chain and added explicit production-only `PAYMENT_MAINNET_ENABLED=true` gating paired strictly with Base mainnet chain ID `8453`. Updated API fixtures and configuration tests. | Local development and testnet operation cannot silently default to Base mainnet or accept an inconsistent mainnet flag. |
| **W — webhook SSRF safety** | Added reusable callback URL validation and DNS-resolution checks that reject loopback, private, link-local, metadata, credential-bearing, unsupported-protocol, and non-standard-port destinations. Hook registration now validates before queuing deliveries. | Extension hooks cannot target internal network addresses through the registration path; delivery remains operator-scoped and signed. |
| **X — retry safety** | Added delivery-time DNS revalidation, exponential retry scheduling, `nextAttemptAt` metadata, and configurable `WEBHOOK_RETRY_BASE_DELAY_MS`. | DNS rebinding is rechecked at delivery; failed deliveries cannot be retried continuously without backoff. |
| **Y — state recovery** | Added versioned snapshot validation, malformed-snapshot quarantine, restrictive `0600` persistence, atomic temporary-file writes, and recovery tests. | Corrupt or unsupported operational snapshots are quarantined rather than silently restored; payment truth remains in PostgreSQL/verifier evidence. |
| **Z — financial and verifier observability** | Added operator-scoped read-only `GET /api/v2/ops/financial/summary` for payment-intent status, durable-stream lifecycle, chain-event finality, ledger count, and unreconciled streams; added `GET /api/v2/ops/verifier/status` for cursor position, cursor age, RPC configuration, and confirmation threshold. | Both reports are explicitly `verifier_owned` and `read_only`; they cannot mutate payment or ledger state. |
| **AA — lifecycle authority hardening** | Removed `mock_adapter` from the canonical payment lifecycle event-source registry and every financial transition. Added regression coverage that rejects mock settlement authority while preserving verifier and ledger-worker transitions. | Test or simulation code cannot authorize economic state transitions through the production lifecycle contract; only explicit wallet, API, verifier, ledger-worker, and operations authorities remain. |
| **AB — legacy mutation quarantine** | Added a reusable production guard to all legacy in-memory payment mutation routes: stream creation, confirmation, withdrawal, cancellation, and dispute mutation/resolution. Durable v2 APIs remain the production path. | Production cannot mutate financial state through the legacy in-memory route family; non-production tests retain compatibility and the denial is explicit and auditable. |
| **AC — durable summary correctness** | Corrected financial observability to group streams by durable `lifecycle_state` and scope payment-intent counts to the configured settlement chain. Updated regression coverage for the operator summary. | Operator metrics no longer report legacy stream status as durable lifecycle truth or mix payment-intent counts across unsupported chains. |
| **AD — verifier freshness gate** | Added bounded `VERIFIER_CURSOR_MAX_AGE_MS` configuration and evidence-based release-readiness gating from the durable verifier cursor. Production readiness now distinguishes fresh, stale, and missing cursors; non-production remains usable without a configured worker. | RPC configuration alone cannot make production shadow-pilot readiness pass; the verifier must have recent durable cursor evidence. |
| **AE — operator verifier status classification** | Extended `GET /api/v2/ops/verifier/status` with explicit `fresh`, `stale`, `missing`, and `not_configured` classifications, readiness boolean, freshness threshold, and an operator-readable reason. | The status remains read-only and verifier-owned; operator dashboards cannot mistake an unconfigured or stale cursor for healthy chain verification. |
| **AF — deployment preflight** | Added `backend:deployment:check`, a read-only configuration preflight that validates production database/JWT/RPC/protocol/webhook/token/mainnet-gate requirements and identifies the deployment target. | The preflight performs no deployment, network call, transaction, or state mutation; it is suitable for Railway trial validation before any explicit deployment approval. |
| **AG — Railway trial gate** | Added `backend:railway:trial:check`, HTTPS trial-URL validation, redacted settings comparison for environment/chain/mainnet mode, and explicit unavailable-settings reporting. | The gate never deploys or calls Railway; missing project settings fail closed as `settings_unavailable` rather than being inferred or fabricated. |
| **AH — reconciliation SLO and alert evidence** | Added bounded `RECONCILIATION_LAG_THRESHOLD_MS` configuration, durable chain-to-ledger projection lag metrics, `fresh`/`lagging`/`missing` projection status, lagging-stream counts, missing-ledger counts, and explicit `ledger_projection_lag` issues to the read-only reconciliation report. | Reconciliation remains read-only and verifier-owned; delayed or missing projections are surfaced for operator action and never silently repaired. |
| **AI — AI ranking quality gate** | Added deterministic shadow-quality evaluation for minimum sample size, baseline improvement, confidence lower bound, rollback target, and explicit human review. Detailed shadow-run evidence now includes the quality-gate result. | Candidate ranking remains permanently `shadow_only`; a quality-gate pass never applies a candidate or changes payment, ledger, outcome, or reputation state. |
| **AJ — collaboration-AI guardrails** | Added provenance-backed advisory evidence with future retention, bounded latency and cost, raw-content exclusion, required human override, and explicit non-settlement authority. | Collaboration intelligence remains advisory, non-applied, attributable, bounded, and independent from payment, ledger, outcome, and reputation transitions. |
| **AK — Flow v3 adverse-event evidence** | Added distinct `stream_refunded` protocol evidence and regression coverage for refund, pause, restart, void, and withdrawal decoding from durable stream context. | Refunds cannot be misclassified as top-ups; adverse events remain verifier evidence and do not invent financial lifecycle states or mutate ledger authority. |
| **AL — final release approval gate** | Added read-only operator `GET /api/v2/ops/release-approval` combining deployment preflight, database/readiness, fresh verifier cursor, clean reconciliation, shadow-review, rollback-target, mainnet-policy, and explicit human-approval evidence. | The artifact fails closed until human approval is supplied; it cannot deploy, promote AI, or mutate settlement, ledger, outcomes, or reputation. |
| **AM — release approval inspection CLI** | Added `backend:release:approval:check`, which emits the same composed approval artifact for CI/Railway preflight review and exits nonzero when approval is blocked. Added `docs/final-production-release-checklist.md` with the final human sign-off gates. | The CLI and checklist are read-only; the observed local result was honestly blocked only because explicit human approval and authenticated Railway settings were absent, while deployment and settlement mutation remained false. |
| **AN — release-candidate manifest** | Added `backend:release:manifest:check`, which fingerprints runtime artifacts, captures the active commit and settlement policy, and verifies clean-worktree and artifact-hash evidence. | The manifest is read-only and fails closed on dirty or incomplete release evidence; it does not deploy or mutate settlement. |
| **AO — complete manual sign-off evidence** | Hardened the release-approval gate so approval requires a reviewer identity, valid timestamp, `production_release` scope, and explicit rollback acknowledgement in addition to an approved decision. | Incomplete or ambiguous sign-off remains blocked; no deployment, AI promotion, or settlement mutation can pass through the artifact. |
| **AP — final pre-flight and signed release payload** | Added `backend:release:payload:check`, Ed25519 canonical payload signing, signature verification tests, migration validation, schema backup/catalog recovery evidence, and Railway configuration comparison. | The payload remains blocked when Railway settings, full recovery evidence, or human approval are unavailable; no deployment or settlement mutation is authorized. |
| **AQ — detached payload verification hardening** | Added `backend:release:payload:verify`, independent evidence-gate evaluation, exact canonical-evidence comparison, Ed25519 algorithm enforcement, canonical-envelope tamper rejection, and read-only CLI coverage. | Verification does not trust the payload status envelope, cannot accept incomplete release evidence, and never deploys or mutates settlement. |
| **AR — operator financial audit visibility** | Added read-only `GET /api/v2/ops/audit/events` with bounded pagination, financial filters, durable ordering, and recursive sensitive-metadata redaction. | Audit access is scoped to `ops:*`, reads only `financial_audit_events`, and cannot mutate financial state or expose private credentials. |
| **AS — settlement token-registry consistency** | Added production-only validation that enabled settlement tokens match the configured chain, protocol contract, unique symbol set, and valid ERC-20 decimals. | A token cannot silently route to an inconsistent chain or protocol contract; development remains compatible while production preflight fails closed. |
| **AT — discovery outcome lineage** | Added read-only `GET /api/v2/ops/discovery/lineage` linking impressions to engagements and verified/unverified/rejected outcomes with ranking provenance and no raw query content. | Ranking evidence is traceable to verifier-owned outcomes while collaboration content remains excluded and no AI or payment state is mutated. |
| **AU — reproducible recovery evidence** | Added `backend:recovery:check`, protected custom-format backup creation, catalog counting, SHA-256 fingerprinting, explicit isolated-target guard, and restored schema/migration verification. | Recovery never overwrites the source database; it reports `schema_catalog_only` until a separate target restores successfully and reports `verified`. |
| **AV — verifier operations evidence** | Added `backend:verifier:operations:check`, `GET /api/v2/ops/verifier/operations`, cursor/finality/reconciliation/audit composition, and fail-closed readiness. | Evidence remains read-only and verifier-owned; `not_configured`, stale, missing, unlinked, or reconciliation-attention states block readiness. |
| **AW — route contract hardening** | Added degraded-database and operator-scope integration coverage for audit, lineage, and verifier-operations routes. | Operator evidence endpoints cannot return fabricated empty reports when PostgreSQL is unavailable. |
| **AX — read-only ERC-20 metadata validation** | Added `backend:token:metadata:check` and provider-based symbol/decimals/chain comparison against the registry. | The probe performs no chain mutation and blocks RPC chain mismatches, unreadable tokens, symbol mismatches, and decimals mismatches. |
| **AZ — lineage-backed evaluation export** | Extended evaluation examples with rank position, selected state, lineage status, outcome IDs/counts, coverage metrics, and raw-content exclusion. | Only verified outcomes become eligible labels; unverified/rejected/unlinked records remain visible as shadow evidence. |
| **BC — controlled Base Sepolia smoke harness** | Hardened `backend:smoke:phase2:check` with isolated-database requirement, Base Sepolia/mainnet guard, configured-token requirement, ephemeral wallets, cleanup, and explicit no-chain-transaction output. | The harness fails closed outside an explicitly isolated disposable database and never claims settlement from an API action. |
| **BD — v2 operations contract documentation** | Added versioned contracts for audit, lineage, verifier operations, recovery, token metadata, and controlled smoke paths. | Integration consumers receive explicit authority, scope, degraded-state, pagination, and mutation semantics. |
| **AY — durable outbox and operator delivery health** | Added durable outbox enqueueing in verifier API/worker projections, leased claiming, bounded retry/failure recording, dead-letter classification, payload fingerprints, `backend:outbox:health:check`, `GET /api/v2/ops/outbox/health`, and `GET /api/v2/ops/outbox/events`. | Delivery remains downstream of verifier authority; dead-letter health is visible without returning payload bodies or changing settlement state. |
| **BA — bounded advisory-AI provider and retrieval boundary** | Added provider/model capability reporting, content-free retrieval evidence, source-event provenance, retention, latency/cost budgets, human-review requirement, shadow-only status, `backend:advisory:ai:check`, `GET /api/v2/intelligence/advisory/capabilities`, and `POST /api/v2/intelligence/advisory`. | The default provider is absent and invocation fails closed; any future provider result remains advisory, non-persistent for raw content, non-applied, and unable to mutate payment or ranking state. |
| **BB — collaboration degraded-state health** | Added `GET /api/v2/collaboration/health` with independent engagement-store, session-auth, realtime-transport, payment, verifier, and indexer checks. | Payment/verifier/indexer degradation keeps collaboration available with `paymentStateMayBeStale: true`; core store/auth failure blocks collaboration; no settlement authority is granted. |
| **AW — ready-PostgreSQL route contracts** | Extended `backend:ready:postgres:check` to require an explicit isolated database, create a disposable engagement, exercise BG payment-state, register v2 extensions, inspect audit/lineage/outbox/verifier evidence, and call BH outbox processing in dry-run mode. | Without isolation it exits `1` before database access; the disposable CI database returned `status: verified`, with BG/BH mutation and settlement checks remaining read-only. BF reviewer writes remain excluded so the verifier cannot fabricate human decisions. |
| **BD — public extension schemas** | Added v2 extension contract capabilities, allowlisted events, bounded projections, replay windows, signed/retryable delivery metadata, raw-content stripping, and owner-scoped hook registration/listing. | Extension payloads are versioned and content-limited; delivery cannot establish settlement or mutate financial state. |
| **BF — durable reviewer-decision audit records** | Extended `reviewShadowRun` so first and replayed human decisions create `financial_audit_events` records with `operator` authority, immutable action names, model/baseline context, privacy-preserving reviewer-note hashes, replay markers, and same-transaction outbox events. | Decisions remain `applied: false`, `promotionStatus: shadow_only`, `authority: human_review_required`, and `mutation: read_only`; reviewer note text is never copied into the financial audit metadata or outbox payload. |
| **BG — verifier-owned engagement payment-state surface** | Added `GET /api/v2/engagements/:engagementId/payment-state`, which composes authenticated engagement access, latest intent/stream, finality, ledger-entry evidence, and verifier-cursor freshness into exact `lifecycle_state`, `finality_status`, `payment_status`, and `paymentStateMayBeStale` fields. | The route is read-only and verifier/ledger-labeled; missing or stale chain evidence is explicit and never blocks collaboration or claims settlement. |
| **BH — durable outbox delivery processor** | Added `POST /api/v2/ops/outbox/process` with dry-run default, non-mutating candidate planning, leased durable claims, matching v2 extension-hook delivery, exact-body HMAC signatures, timestamp tolerance, stable event identifiers, SSRF-safe callback validation, bounded retry, and dead-letter classification. | Delivery is `durable_outbox_delivery` only; no callback or processor action establishes settlement, changes ledger state, or promotes AI. Consumers can use the shared verifier for constant-time digest checks, timestamp skew rejection, and replay-key protection. |
| **BJ — webhook replay-load validation and shared-store guidance** | Added a 100-event isolated delivery test that captures simulated callbacks, independently verifies every exact HMAC, and rejects all duplicate replay attempts. Added PostgreSQL/Redis atomic-claim guidance, fail-closed store behavior, crash/retry states, and multi-instance requirements. | The test uses no live callback, funds, chain, or settlement path. The in-process guard remains explicitly non-authoritative and insufficient as the sole production store across multiple instances. |
| **BL — durable housekeeping and replay claims** | Added bounded `backend:idempotency:cleanup` housekeeping, migration `014_webhook_replay_claims`, atomic PostgreSQL replay claims, and a shared verify-before-claim webhook signature boundary. | Expired idempotency records are deleted in `FOR UPDATE SKIP LOCKED` batches; durable replay claims fail closed on duplicates/store errors and never establish settlement. |
| **BM — durable verified trust signals** | Added migration `015_verified_trust_signals`, verifier-only derivation from verified outcomes, participant-specific positive evidence, neutral dispute evidence, idempotent inserts, and a read-only operator listing. | Signals are permanently `eligibleForRanking: false`, `promotionStatus: shadow_only`, and `settlementAuthority: false`; unverified outcomes derive nothing. |
| **BN — durable webhook inbox** | Added migration `016_webhook_inbox` with atomic first-claim, lease reclaim, processed/retryable/quarantined states, bounded retries, raw-content rejection, and inbox health evidence. | Valid signed deliveries can be processed at least once with crash-safe recovery; no inbox state establishes settlement. |
| **BO — durable hooks and production outbox worker** | Added migration `017_extension_hooks`, PostgreSQL-backed v2 hook registration/listing, explicit worker opt-in, bounded polling/leases/batches, graceful stop, separate worker entrypoint, and operator webhook-inbox health. | The worker loads durable hooks after restart, requires a production signing secret, and delivers only through the existing bounded outbox processor; inbox health is read-only delivery evidence and never settlement authority. |
| **BP — public OpenAPI and extension SDK** | Added public `/api/v2/extensions/openapi.json`, runtime-to-OpenAPI contract verification, a dependency-free `@paytray/sdk` workspace with TypeScript declarations, HTTPS registration helper, structured API errors, and read-only safety metadata. | The public document is descriptive only; SDK methods are limited to v2 capability discovery and owner-scoped hook operations, with no payment, ledger, settlement, or AI-promotion mutation surface. |
| **BQ — explicit idempotency-cleanup schedule** | Added `backend:idempotency:cleanup:run`, read-only `backend:idempotency:cleanup:check`, opt-in `IDEMPOTENCY_CLEANUP_ENABLED`, bounded 15-minute/500-record defaults, production-time override rejection, and external-host scheduler guidance. | Cleanup remains limited to expired idempotency records using PostgreSQL `FOR UPDATE SKIP LOCKED`; it cannot mutate payment, ledger, verifier, trust, webhook-inbox, or settlement state. |
| **BR — unified target-operations preflight** | Added read-only `backend:target:operations:check`, composing deployment configuration, redacted Railway settings comparison, Base Sepolia policy, HTTPS RPC, outbox-worker opt-in, idempotency-housekeeping opt-in, and named blockers. | The report always emits `releaseEligible: false`; configuration cannot substitute for durable verifier freshness, backup-restore evidence, reconciliation, human approvals, or signing-key custody. |

## Validation

The latest BM/BN/BO/BP/BQ/BR/BS/BT/BU hardening tranche passed **58 test files and 250 tests**, including verifier-only trust-signal derivation and replay backfill, durable webhook inbox claim/reclaim/retry/quarantine coverage, durable PostgreSQL hook persistence, worker idle/single-flight coverage, public OpenAPI route and SDK request/error coverage, a runtime-to-OpenAPI contract check, fail-closed/default and explicitly enabled housekeeping-schedule checks, target-operations blocked/ready/mismatch coverage, verifier-worker configuration coverage, bounded rate-limit and HTTP trust-boundary coverage, a 100-event simulated delivery/replay-load test, exact outbound HMAC recomputation, malformed/tampered signature rejection, timestamp skew rejection, PostgreSQL migration code validation through migration 017, ESLint, and `git diff --check`. The extended isolated verifier returned `status: verified` with `engagementPaymentState: true`, `extensionOpenApi: true`, `trustSignals: true`, `extensionRegistration: true`, `webhookInbox: true`, and `outboxDryRun: true`; the worker configuration verifier returned `status: ready` with explicit opt-in and signing-secret requirements; verifier operations remained an intentional structured `503` because no target verifier cursor was configured. All release artifacts remain non-deploying and non-settling.
 The local backup procedure verified migration/schema catalog recoverability only; a full restore into an isolated database remains a manual release gate. The AU catalog-only check, AV operations report, AX metadata check, BC smoke harness, and BA advisory capability check exit nonzero as designed when target recovery, RPC, isolation, or provider evidence is absent; the disposable AU restore returned `status: verified` and exit `0`. The AP payload and AQ verifier continue to exit 1 as designed when approval, Railway settings, recovery evidence, or a complete signed payload are absent. All release artifacts reported `deploymentPerformed: false` and `settlementMutationPerformed: false`.

## Railway development-server trial context

The user identified the following Railway project as a development-server trial for testing or possible deployment: [Railway project](https://railway.com/project/013770d1-6f03-48ae-a346-cc7dcce1629b?). The project page exposed a Railway login/404 shell rather than authenticated service settings, and a subsequent browser inspection timed out. Therefore, no actual Railway environment values were asserted. The local comparison correctly reported `settings_unavailable`. This is recorded as **deployment context only**. No Railway login, deployment, environment mutation, production rollout, mainnet transaction, live fund movement, or real-user-data operation was performed during this batch.

Before any Railway deployment, the project still requires explicit deployment approval, production environment review, HTTPS RPC configuration, token-registry verification, database migration execution, verifier-cursor freshness validation, smoke tests, and confirmation that settlement remains Base Sepolia unless the separately gated mainnet policy is deliberately approved.

## Remaining batch path

| Planned batch | Priority scope | Exit condition |
|---|---|---|
| **AG — completed locally** | Added the non-deploying Railway trial gate and honest unavailable-settings result. | No Railway settings were inferred; deployment remains blocked until authenticated settings evidence and explicit approval are available. |
| **AH — completed locally** | Added bounded discrepancy age and ledger-projection lag evidence to the read-only reconciliation surface. | Operators can distinguish fresh, delayed, and missing financial projections without any automatic repair. |
| **AI — completed locally** | Added minimum sample, baseline improvement, confidence, rollback-target, and human-review quality checks to detailed shadow-run evidence. | No candidate ranking is promoted; the response remains `shadow_only` and `human_review_required`. |
| **AJ — completed locally** | Added provenance, retention, cost, latency, raw-content, and human-override controls for collaboration AI advisory evidence. | AI assistance remains advisory, attributable, bounded, and independent from settlement authority. |
| **AK — completed locally** | Added distinct Flow v3 refund evidence and coverage for refund, pause, restart, void, and withdrawal decoding. | Adverse protocol evidence is explicit and verifier-owned; no automatic ledger repair or lifecycle invention occurs. |
| **AL — completed locally** | Added the final read-only release approval artifact and operator endpoint, which fails closed until explicit human approval. | No deployment, AI promotion, or mainnet settlement change can occur through the artifact. |
| **AM — completed locally** | Added the read-only release-approval inspection CLI for CI/Railway preflight workflows. | A blocked CLI exit is an intentional safety signal, not a deployment failure or state mutation. |
| **AN — completed locally** | Added the read-only release-candidate manifest and artifact fingerprint CLI. | Dirty or incomplete release evidence blocks the manifest; no deployment or settlement mutation occurs. |
| **AO — completed locally** | Hardened human approval evidence requirements in the final release gate. | Approval requires reviewer, timestamp, release scope, rollback acknowledgement, and approved decision; otherwise the artifact remains blocked. |
| **AP — pushed** | Added the fail-closed Ed25519 signed release payload, migration/schema backup catalog evidence, and Railway configuration status checks. | Payload signing is not a deployment action; incomplete recovery or unavailable Railway settings keep the payload blocked. |
| **AQ — completed locally** | Added detached signed-payload verification with independent evidence checks and canonical-envelope tamper rejection. | Verification remains read-only and fail-closed; no incomplete or tampered payload can be treated as a production release. |
| **AR — completed locally** | Added scoped, paginated, filtered financial audit-event visibility with sensitive-metadata redaction. | Operators can inspect verifier and ledger evidence without granting mutation authority or leaking secrets. |
| **AS — completed locally** | Added strict production settlement token consistency checks. | Production preflight blocks missing or mismatched protocol/chain metadata; Base Sepolia remains the safe default. |
| **AT — completed locally** | Added impression-to-engagement-to-outcome lineage reporting with verification status and provenance. | Ranking evaluation can trace labels to durable outcomes without exposing raw collaboration content. |
| **AU — completed locally** | Added reproducible custom-format backup, SHA-256 fingerprint, catalog evidence, and isolated restore verification. | Disposable restore returned `verified`; target environment recovery remains a release gate. |
| **AV — completed locally** | Added verifier operations CLI and operator endpoint with fresh-cursor/reconciliation/audit gating. | Current local CLI is blocked `not_configured` until the target RPC worker and cursor are real. |
| **AX — completed locally** | Added read-only RPC token metadata comparison. | Local mismatch and unreadable-token tests pass; target RPC evidence remains pending. |
| **AZ — completed locally** | Added explicit evaluation-lineage coverage and verified-only label provenance. | Raw collaboration content is excluded and non-verified labels remain shadow-only. |
| **BC — completed locally** | Added isolated Base Sepolia smoke guardrails, ephemeral wallets, cleanup, and no-chain-mutation output. | The smoke harness fails closed without explicit isolation and configured token evidence. |
| **BD — pushed** | Added versioned v2 operator/lineage/recovery/token/smoke contract documentation and public extension schemas. | Consumers and extension owners have explicit authority, scope, projection, replay, pagination, and degraded-state behavior. |
| **BB — pushed** | Added collaboration health with independent payment/verifier/indexer degradation and core dependency blocking semantics. | Collaboration remains available during payment degradation and reports stale-payment risk without inferring settlement. |
| **AW — pushed** | Added an isolated ready-PostgreSQL route-contract verifier across operator, lineage, outbox, collaboration, and extension routes. | No-isolation mode exits before DB access; the disposable CI target returned `status: verified`. |
| **AY — completed locally** | Added durable outbox records at verifier API/worker projection boundaries, bounded retry states, dead-letter health, and redacted operator inspection. | Delivery evidence is durable and read-only; it cannot establish or mutate settlement. |
| **BA — completed locally** | Added advisory provider/retrieval boundary, budgets, provenance, retention, human override, raw-content exclusion, and fail-closed capability/invocation surfaces. | No default provider is enabled; future provider output remains `shadow_only` and non-authoritative. |
| **BF — pushed** | Added durable audit evidence for first and replayed shadow-review decisions, with privacy-preserving note hashes, immutable operator action metadata, and same-transaction outbox events. | Every decision is visible through the financial audit surface without copying note text or applying/promoting a candidate. |
| **BG — pushed** | Added authenticated engagement payment-state read model with verifier lifecycle/finality, ledger evidence, cursor freshness, and stale-state flag. | Collaboration remains independent from payment dependency health; the endpoint never claims settlement from intent or stale evidence. |
| **BH — pushed** | Added operator dry-run/process endpoint over the durable outbox and v2 extension hooks with exact-body HMAC signing, bounded timestamp freshness, stable event identifiers, SSRF-safe delivery, and retryable processing. | Processing is downstream of verifier authority, bounded, and unable to mutate settlement, ledger, or AI promotion state; consumers now have shared replay-protection primitives. |
| **BJ — pushed** | Added 100-event simulated delivery/replay-load coverage and a shared durable replay-store integration guide for PostgreSQL and Redis. | Duplicate claims are rejected in the isolated load test; horizontal production consumers still require an atomic shared durable inbox/replay store. |
| **BP — pushed** | Added the versioned public OpenAPI document and dependency-free extension SDK. | Contract and SDK validation pass; commit `d63c046` is pushed, and the surface remains descriptive/read-only with no settlement authority. |
| **BQ — pushed** | Added the explicit production idempotency-cleanup run/check commands and external scheduler contract. | Default configuration fails closed; enabled production-like verification reports a bounded 15-minute/500-record schedule without executing cleanup; commit `7f7a264` is pushed. |
| **BR — pushed** | Added the unified target-operations configuration preflight. | Default settings block, matched redacted target settings can report configuration `ready`, explicit Railway mismatches block, and every result remains `releaseEligible: false` until genuine target evidence and approvals exist; commit `de44809` is pushed. |
| **BS — pushed** | Added the explicit Base Sepolia verifier-worker entrypoint and read-only configuration verifier. | Worker startup requires explicit opt-in, HTTPS RPC, matching Sablier/token configuration, durable PostgreSQL cursor projection, bounded polling, and graceful shutdown; no live worker was started; commit `6e857f2` is pushed. |
| **BT — pushed** | Added shared `backend:quality:check` and a Node 22/PostgreSQL 16 GitHub Actions quality workflow. | Local and CI paths cover locked install, full tests, ESLint, extension contract, migrations, and isolated ready-PostgreSQL routes without live-chain mutation; commit `6e857f2` is pushed. |
| **BU — pushed** | Extended target-operations readiness with verifier-worker checks and hardened HTTP/rate-limit controls. | Explicit proxy trust, bounded request bodies, expired-key eviction, and key-budget tests pass; target preflight remains `releaseEligible: false`; commit `6e857f2` is pushed. |

## Commit and push boundary

The AU–BD, AY, BA, BB, AW, and BD implementation tranches remain validated and pushed. BF is pushed at `810868f97ab0b8706dfde5747830d9e935eb7c67`; BG and BH are pushed together at `a40adb4f4a83e15d9806ebefc7047641085784d6`; BI is pushed at `124701ba78d79d96f2abd51ccd59580e9db86a49`; BJ is pushed at `210f2573025ef1fe7bbff2965fc63172ac3b68f6`; BL is pushed at `cbbec7311f8e98547226af6a50567e338ac91601`; BM, BN, and BO are pushed together at `cffc9c2`; BP is pushed at `d63c046`; BQ is pushed at `7f7a264`; BR is pushed at `de44809`; and BS, BT, and BU are pushed together at `6e857f2`.


No deployment, mainnet transaction, live funds, or real user data was used. The W–Z tranche is pushed at commit `3a44dbf0034945f84e5c0e8b885e3cd88a32db5b`, Batch AA is pushed at `b513aabe4a4043210dc9278d83c3c5b7be836735`, Batch AB is pushed at `787c77ef39c2a704cc51a831e83e6abebde22c6c`, Batch AC is pushed at `23194e876656d8b3b05be24774498d3cc635eee2`, Batch AD is pushed at `f42feecb38ff2f8b3761648f7a3ca38648af8595`, Batch AE is pushed at `64a6094822e6770355f7ea157b9ad84411df883f`, Batch AF is pushed at `ada79e64da37d20a3035984f8cf9799e217ce9e0`, Batch AG is pushed at `f9b5f842837c593d72d971f12781b9488c623ab6`, Batch AH is pushed at `c44703bbfb1e52ff806e2006f08f919da704c23e`, Batch AI is pushed at `405f11eb800e0b7ac5155c9255a34a83e9d1faae`, Batch AJ is pushed at `de91f5b41bc662d69ef515ac1b64e15d787448d5`, Batch AK is pushed at `e1749c3e1b6f673f8580fab962b0aba77ffcdb5a`, Batch AL is pushed at `65ee66dc084a01d8c62c64935a2cd7d976295f75`, Batch AM is pushed at `17f77b17f5c551c04ab57c32ca6d9e90d37be1b0`, Batch AN is pushed at `9d6f17fb601e9ec2909500f1c4a43b3378e71ad5`, Batch AO is pushed at `f8fe980d2b67ca8ea3a8693b3658f21775199b22`, Batch AP is pushed at `19a78e0a5bf53ea9e63f70af74b470a8e330d06e`, Batch AQ is pushed at `c447a5218bb221deac08df4b490ac56dbe27ff8e`, and Batch AR–AT are pushed at `16165d61b5f1192fb747b31ea3783b65e1e6326f`, and Batch AU–BD, AY, and BA are pushed at `6465f31086e37263208ca2ec6f3794c8c958f8a4`.
